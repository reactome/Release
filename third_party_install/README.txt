This directory contains generic configuration files to facilitate
third-party installations of reactome.

Once reactome is installed in the path:
/usr/local/reactome/Reactome/production

The config files can be installed (as root)

cd /
tar zxvf /usr/local/reactome/Reactome/production/GKB/third_party_install/config.tar.gz 

